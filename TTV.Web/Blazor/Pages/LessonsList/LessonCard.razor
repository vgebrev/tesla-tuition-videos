@using Microsoft.Extensions.Options;
@using TTV.Web.Shared;
@inject IOptionsSnapshot<LocalConfig> Config

<div class="card border-primary h-100">
    <div class="card-header bg-secondary"><h5 class="card-title text-center mb-0">@Lesson.Title</h5></div>
    <div class="card-body">
        <div class="row">
            <div class="col-12">
                <a href="lesson/@Lesson.Id"><img class="rounded-4 w-100" src="@ThumbnailUri" alt="@Lesson.Title" title="@Lesson.Title" /></a>
            </div>
        </div>
        <p class="card-text">@Lesson.Description</p>
        <TagBadges Tags="Lesson.Tags"></TagBadges>
    </div>
    <div class="card-footer d-flex justify-content-between align-items-baseline">
        <a href="lesson/@Lesson.Id" class="card-link">View Lesson</a>
        <LessonOwnView Lesson="Lesson">
            <Owned>
                <span class="text-primary text-sm">You own this lesson</span>
            </Owned>
            <NotOwned>
                <BuyNowButton Lesson="Lesson"></BuyNowButton>
            </NotOwned>
        </LessonOwnView>
    </div>
</div>

@code {
    [Parameter]
    public LessonDto Lesson { get; set; } = new();

    private string ThumbnailUri => $"{Config.Value.ApiRootUri}/api/video/{Lesson.Id}/thumbnail";
}
